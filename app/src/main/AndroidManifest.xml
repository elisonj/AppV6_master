<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.bg7.appvistoria">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <!-- TODO: Usar o icone correto -->
    <!-- TODO: Usar allowBackup=true e especificar dados de backup -->
    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:allowBackup="false"
        android:supportsRtl="true"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".product.ProductActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|locale">

        </activity>

        <activity android:name=".config.ConfigActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|locale">

        </activity>
        <activity android:name=".projectselection.ProjectSelectionActivity"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBar"
            android:configChanges="orientation|locale">

        </activity>

        <activity
            android:name=".login.LoginActivity"
            android:configChanges="orientation|locale"
            android:theme="@style/NoActionBar"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>